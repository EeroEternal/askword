import React from 'react';
import ReactDOM from 'react-dom/client';
import './main.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <h2 className='bg-blue-500 text-center text-white'>Hello from React!</h2>
  </React.StrictMode>
);
